{
  "name": "package-parser",
  "version": "1.0.0",
  "description": "package for personal needs, basicly all it does is parse given package url for metadata and downloads it; it also is capable of building dependecies graph with depth of 4",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shredder_senpai",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "cheerio": "^1.0.0-rc.3",
    "puppeteer": "^5.3.1",
    "request": "^2.88.2",
    "request-promise": "^4.2.6",
    "yargs": "^16.0.3"
  }
}
